# 示例：包含多余空格和过长行
def hello():
    print(
        "This line has extra spaces and is intentionally poorly"
        "formatted to demonstrate linter errors."
        "This line is also very long to trigger a line length warning."
        )
